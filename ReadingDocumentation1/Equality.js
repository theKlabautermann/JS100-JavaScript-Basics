console.log('8' == 8);
console.log('8' === 8);

/*
From MDN:
The equality operator (==) checks whether its two operands are equal,
returning a Bollean result. Unlike the strict equality operator (===),
it attempts to convert and compare operands that are of different types.
*/
