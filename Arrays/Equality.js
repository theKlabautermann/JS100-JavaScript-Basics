//Expected output: false. Despite having the same values, they are different arrays.
//Checking if they are the same Array will therefore return false.

let array1 = [2, 6, 4];
let array2 = [2, 6, 4];

console.log(array1 === array2);